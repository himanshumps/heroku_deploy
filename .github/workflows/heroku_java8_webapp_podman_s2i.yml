name: Heroku Deployment for the java 8 fat jar web application

on:
  workflow_dispatch:
    inputs:
      sourceCodeGitRepo: 
        description: Git repo for the java webapp fat jar application
        required: true
        default: https://github.com/himanshumps/vertx-starter.git
      herokuProjectName:
        description: Heroku project name where the application needs to be deployed
        required: true
        default: vertxdemo23170
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install heroku toolchain
      run: sudo snap install --classic heroku
    - name: Check heroku version
      run: heroku --version
    - name: Build the docker image using s2i light with podman
      run: ./s2i-light.sh build ${{ github.event.inputs.sourceCodeGitRepo }} registry.hub.docker.com/himanshumps/ubi_java8 registry.heroku.com/himanshumps/${{ github.event.inputs.herokuProjectName }}/web -v $(pwd)/maven/:/tmp/maven/settings/:Z
    - name: Login into heroku registry
      run: podman login --username=${{ secrets.HEROKU_EMAIL }} --password=${{ secrets.HEROKU_API_KEY }} registry.heroku.com
    - name: Push the image to heroku registry
      run: podman push registry.heroku.com/himanshumps/${{ github.event.inputs.herokuProjectName }}/web
    - name: Release the new image
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:push -a appname web 
    - name: Release
      env:
        HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
      run: heroku container:login && heroku container:release web -a ${{ github.event.inputs.herokuProjectName }} web 
